# Paper run exports

These nine folders are curated W&B API exports for the checkpoints selected in
the paper. They include configuration, summaries, compressed histories, and
available source snapshots. Raw `.wandb` stores and trained checkpoints are not
included. Original cluster paths in metadata are provenance, not configuration.

